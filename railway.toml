# Railway 配置文件
# 文档：https://docs.railway.com/config-as-code

[build]
# 指定构建目录为 backend 文件夹
root = "backend"

# 使用 Nixpacks 自动构建
builder = "NIXPACKS"

[deploy]
# 启动命令
startCommand = "node index.js"

# 失败时自动重启
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
